@model IEnumerable<Cines_Flagg.Models.UsuarioFuncion>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MiUsuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MiFuncion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.cantidadCompra)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MiUsuario.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MiFuncion.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cantidadCompra)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
